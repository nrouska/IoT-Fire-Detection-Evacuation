<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Camera {{device.id}}</title>

    <style>
        :root {
            --bg: #0f1220;
            --card: #1b1f3b;
            --text: #e6e8ff;
            --muted: #9aa0ff;
            --accent: #6c7bff;
            --radius: 14px;
        }

        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
            Roboto, Ubuntu, sans-serif;
        }

        body {
            margin: 0;
            min-height: 100vh;
            background: radial-gradient(1200px 600px at 50% -200px, #2a2f6f, var(--bg));
            color: var(--text);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 24px;
        }

        .camera-card {
            width: 100%;
            max-width: 960px;
            background: linear-gradient(180deg, #222766, var(--card));
            border-radius: var(--radius);
            box-shadow:
                    0 20px 40px rgba(0, 0, 0, 0.5),
                    inset 0 1px 0 rgba(255, 255, 255, 0.05);
            overflow: hidden;
        }

        .header {
            padding: 16px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }

        .title {
            font-size: 1.1rem;
            font-weight: 600;
            letter-spacing: 0.3px;
        }

        .status {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.85rem;
            color: var(--muted);
        }

        .status-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: #2bff88;
            box-shadow: 0 0 8px rgba(43, 255, 136, 0.8);
        }

        .video-wrapper {
            background: black;
            position: relative;
        }

        video {
            width: 100%;
            height: auto;
            display: block;
            aspect-ratio: 16 / 9;
            background: black;
        }

        .footer {
            padding: 12px 20px;
            font-size: 0.8rem;
            color: var(--muted);
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .device-id {
            font-family: monospace;
            opacity: 0.8;
        }

        @media (max-width: 600px) {
            .title {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>

<div class="camera-card">
    <div class="header">
        <div class="title">Camera {{device.id}}</div>
        <div class="status">
            <span class="status-dot"></span>
            Live
        </div>
    </div>

    {{{ body }}}

    <div class="footer">
        <div>Streaming via HLS</div>
        <div class="device-id">{{device.id}}</div>
    </div>
</div>

</body>
</html>
